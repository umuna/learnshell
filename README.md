# learn-shell

Automate manual effort -> shell scriptting choice

what is shell script:
script is a file, having a list of command to execute. Meaning if we execute that file, all the commands will be executed one after the other

file name extension: .sh(*) , .bash , nome
.sh is widely used extension
However, enabling extension we get advantage editor understanding the code

Hello World
This my first writeup
Added to it 